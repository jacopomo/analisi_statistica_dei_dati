\chapter{Important Distributions}
In this appendix we will highlight the main probability density and mass functions that we will encounter most often during this course, as well as deriving and listing their useful properties. 

For now, here is a framework helped by AI to organize (as you can tell by the titles):

Each distribution we will write, plot, derive (some not all), moments, MLE, support, Moment generating function/characteristic function (some not all), cumulative (some not all), some special stuff for some (exponential period-frequency transformation, for example)

\section{Discrete distributions}
\label{Appendix:discrete}
\subsection{Bernoulli} %Sub sub sections give a nice bold but don't appear in the TOC... Win-win I'd say

\subsection{Binomial}
Support: $k \in \{0, 1, \dots, n\}$ and $p \in [0, 1]$.

The probability of having exactly $k$ successes in $n$ independent Bernoulli trials is:

\begin{equation}
	\mathcal{B}_n(k;p) = \binom{n}{k}p^k\left(1-p\right)^{n-k}
\tag{Binomial pmf}
\label{pmf:binom}
\end{equation}

The Moment Generating Function (MGF) is the most efficient way to derive the raw moments:

	\[
	M_X(t) = \mathbb{E}[e^{tX}] = (1 - p + pe^t)^n
	\]
\todo{Not surem, and not sure how it brings us the moments...}
Which brings us to the mean and variance of $k$:

	\[
	\E\left[k\right] = np \quad \Var\left[k\right] = np(1-p)
	\]

The log-Likelihood for a sample of size $m$ (where each observation $k_i$ is the number of successes in $n$ trials) is:

	\[
	\ln L(p) = \sum_{i=1}^m \left[ \ln\binom{n}{k_i} + k_i\ln p + (n-k_i)\ln(1-p) \right]
	\]

which brings the MLE for $p$:

	\[
	\hat{p}_{MLE} = \frac{1}{mn} \sum_{i=1}^m k_i = \frac{\bar{k}}{n}
	\]

Which is an unbiased estimator with variance:

	\[
	\Var\left[\hat{p}_{MLE}\right] = \frac{1}{n^2}\Var[k] = \frac{np(1-p)}{n^2} = \frac{p(1-p)}{n}
	\]


Fisher Information: This measure tells us how much "information" $k$ carries about the unknown parameter $p$. For a single observation:

	\[
	\mathcal{I}(p) = -\E\left[ \frac{\partial^2 \ell}{\partial p^2} \right] = \frac{n}{p(1-p)}
	\] 

This shows us at a glance that $\hat{p}_{MLE}$ is the minimum variance estimator.

\subsection{Multinomial}
Add the example of the k-median here (distribution of the k-biggest value, it's an important exercise we did in class you'll see)
\subsection{Poisson}

\label{eq:poisson_mle}
We now want to find the maximum likelihood estimator (MLE) for a Poisson distribution. 
Given a sample of $N$ independent observations $\{x_i\}$, the total likelihood is:
\begin{align*}
	L_{\text{tot}}(\mu) &= \prod_{i=1}^N \frac{\mu^{x_i} e^{-\mu}}{x_i!}
\end{align*}
Taking the logarithm, we obtain the log-likelihood:
\begin{align*}
	\ln L_{\text{tot}}(\mu) 
	&= \sum_{i=1}^N \ln \left( \frac{\mu^{x_i} e^{-\mu}}{x_i!} \right) \\
	&= \sum_{i=1}^N x_i \ln \mu - \sum_{i=1}^N \mu - \sum_{i=1}^N \ln(x_i!)
\end{align*}
Since the last term does not depend on $\mu$, it can be neglected. Therefore,
\[
\ln L_{\text{tot}}(\mu) = \sum_{i=1}^N x_i \ln \mu - N\mu
\]

To find the MLE, we solve the \ref{eq:likelihood}:
\[
\frac{\partial \ln L_{\text{tot}}(\mu)}{\partial \mu} 
= \sum_{i=1}^N \frac{x_i}{\mu} - N = 0
\]
Evaluating at $\mu = \hat{\mu}$, we obtain:
\begin{equation*}
	\hat{\mu} = \frac{1}{N} \sum_{i=1}^N x_i
	\label{eq:poissonian_mle}
	\tag{Poisson MLE}
\end{equation*}

As expected, the MLE for the Poisson mean coincides with the sample mean. In the simple case of a single observation of $n$ events, we trivially get: $\hat\mu=n$.
\todo{I wrote this whole section using $\mu$ as the parameter instead of $\theta$ because I feel uneasy using $\theta$ for the expected events of a Poissonian, it just feels wrong. If we want to keep our convention going I can change it, no problem.}
%Negative Binomial / Geometric: For trials until success. Add if we ever use

\section{Continuos distributions}

\subsection{Uniform}\label{sec:uniform}

The general Uniform distribution $\mathcal{U}[l, u]$ with $l, u \in \mathbb{R}$ has the pdf:
\[
p(x; l, u)=
\begin{cases}
	\frac{1}{u-l} & \text{if } x \in [l, u] \\
	0 & \text{otherwise}
\end{cases}
\]

One can equivalently parameterize the distribution in terms of the interval length $\delta$ and its center $\mu$:
\[
p(x; \delta, \mu)=
\begin{cases}
	\frac{1}{\delta} & \text{if } x \in [\mu-\delta/2, \mu+\delta/2] \\
	0 & \text{otherwise}
\end{cases}
\]

To simplify the following derivations, we consider a Uniform $\mathcal{U}[0, m]$ (any other case can be reduced to this one through a simple change of variables).

The expected value is:
\begin{align*}
	\E[x]
	&=\int_{-\infty}^{+\infty} x\,p(x;0,m)\,dx
	=\int_{0}^{m} x\frac{1}{m}\,dx
	=\frac{m}{2}
\end{align*}

and the variance:
\begin{align*}
	\operatorname{Var}[x]
	&=\E[x^2]-\E[x]^2 \\
	&=\int_{0}^{m} x^2\frac{1}{m}\,dx - \frac{m^2}{4} \\
	&=\frac{m^2}{3}-\frac{m^2}{4}
	=\frac{m^2}{12}
\end{align*}

The characteristic function is:
\[
\phi(t)=\E[e^{itx}]
=\frac{e^{imt}-1}{imt}
\]

We now compute the likelihood $L_x(m)$ for a single observation $x$ (see example \ref{ex:uniform_likelihood}):
\[
L_x(m)=\frac{1}{m}\,\mathbb{I}_{\{m \geq x\}}, \quad m>0
\]

We note that the domain of the observable $x$ is clearly dependent on the parameter $m$, thus, to compute the Fisher information, we resort to it's definition \ref{def:fisher_information}:
\[
I(m)=\E \left[\left(\frac{\partial \ln L_x(m)}{\partial m}\right)^2 \right]
=\frac{1}{m^2}
\]

To find the MLE for $m$, we observe that the likelihood is decreasing in $m$ for $m \ge x$ (see fig \ref{fig:uniform_likelihood}), hence its maximum is attained at the boundary:
\[
\hat{m}=x
\]

Given $N$ independent observations $\{x_i\}$ drawn from a uniform distribution, we have:
\begin{align*}
	L_{\text{tot}}(m)
	&=\prod_{i=1}^N\frac{1}{m}\mathbb{I}_{\{m\geq x_i\}}\\
	&=\frac{1}{m^N}\mathbb{I}_{\{m\geq \max_i [x_i]\}}
\end{align*}

The log-likelihood (for $m \ge \max_i [x_i]$) is:
\[
\ln L_{\text{tot}}(m) = -N \ln m
\]
then:
\[
I_{\text{tot}}(m)=\E \left[\left(\frac{\partial \ln L_{\text{tot}}(m)}{\partial m}\right)^2 \right]=\frac{N^2}{m^2}
\]
If you're puzzled by the fact that the Fisher Information is not linear in the sample size $N$ remember that this property emerges in the case of sufficient regularity we talked about when we first introduced the concept of the Fisher Information (see \ref{def:fisher_information}). As we previously said, in the case of uniform distribution, where the bound is dependent on the parameter, those regularity conditions do not hold.

By the same argument as before, the MLE is:

\[
	\hat m = \max_i [x_i]
\]

Further treatment of this fundamental statistic, and it's sufficiency, can be found in example \ref{ex:uniform_sufficient_statistics}.

It is often useful to determine the distributions of 
\[
x_{\max} = \max_i [x_i]
\quad \text{and} \quad
x_{\min} = \min_i [x_i]
\]

We start with $x_{\max}$. Its distribution can be derived in two equivalent ways:

\begin{itemize}
	\item Using the cumulative distribution function:
	\[
	F_{x_{\max}}(x)=P(x_{\max}\leq x)
	\]
	The condition $x_{\max}\leq x$ means that all observations satisfy $x_i \leq x$, hence:
	\[
	P(x_{\max}\leq x)=P(x_1\leq x, \dots, x_N \leq x)
	=\prod_{i=1}^{N}P(x_i\leq x)
	\]
	where we used independence.
	
	For the uniform distribution:
	\[
	P(x_i\leq x)=
	\begin{cases}
		0 & \text{if } x<0\\
		\frac{x}{m} & \text{if } 0\leq x \leq m\\
		1 & \text{if } x> m
	\end{cases}
	\]
	
	Thus, for $x\in [0,m]$:
	\[
	F_{x_{\max}}(x)=\left(\frac{x}{m}\right)^N
	\]
	
	Differentiating:
	\[
	p_{x_{\max}}(x)=\frac{d}{dx}F_{x_{\max}}(x)
	=
	\begin{cases}
		\frac{N}{m}\left(\frac{x}{m}\right)^{N-1} & \text{if } 0 \leq x \leq m\\
		0 & \text{otherwise}
	\end{cases}
	\]
	
	\item Another way to arrive at the same result is to view the problem in terms of a multinomial experiment. 
	
	We consider $N$ independent draws and imagine ordering them. Fix a value $x$ and focus on the $k$-th order statistic $x_k$. Each observation can fall into one of three categories:
	\begin{itemize}
		\item it lies below $x$
		\item it falls in an infinitesimal interval $[x, x+dx]$
		\item it lies above $x$
	\end{itemize}
	
	These three outcomes have probabilities:
	\[
	p_{<} = F(x), \qquad
	p_{=} = p(x)\,dx, \qquad
	p_{>} = 1 - F(x)
	\]
	
	For $N$ trials, the probability that exactly $k-1$ points lie below $x$, one point falls in $[x, x+dx]$, and the remaining $N-k$ lie above $x$ is given by the multinomial distribution:
	\[
	\frac{N!}{(k-1)!(N-k)!1!}
	p_{<}^{\,k-1}\,
	p_{=}\,
	p_{>}^{\,N-k}
	\]
	
	Dividing by $dx$, we obtain the density of the $k$-th order statistic:
	\[
	p_{x_k}(x)
	=
	\frac{N!}{(k-1)!(N-k)!}
	[F(x)]^{k-1}
	[1-F(x)]^{N-k}
	p(x)
	\]
	
	In the case of the maximum, we set $k=N$. Then there are $N-1$ points below $x$ and none above, giving:
	\begin{align*}
		p_{x_{\max}}(x)
		&= N [F(x)]^{N-1} p(x) \\
		&= N \left(\frac{x}{m}\right)^{N-1}\frac{1}{m} \\
		&= \frac{N}{m}\left(\frac{x}{m}\right)^{N-1}
	\end{align*}
	
	which coincides with the result obtained via the cumulative distribution.
\end{itemize}

We now turn to $x_{\min}$. Its distribution can be derived in analogous ways:

\begin{itemize}
	\item Using the cumulative distribution function:
	\[
	F_{x_{\min}}(x)=P(x_{\min}\leq x)
	\]
	It is often more convenient to compute the complementary probability:
	\[
	P(x_{\min} > x)
	\]
	The condition $x_{\min} > x$ means that all observations satisfy $x_i > x$, hence:
	\[
	P(x_{\min} > x)=P(x_1 > x, \dots, x_N > x)
	=\prod_{i=1}^{N}P(x_i > x)
	\]
	
	For the uniform distribution:
	\[
	P(x_i > x)=
	\begin{cases}
		1 & \text{if } x<0\\
		1-\frac{x}{m} & \text{if } 0\leq x \leq m\\
		0 & \text{if } x> m
	\end{cases}
	\]
	
	Thus, for $x\in [0,m]$:
	\[
	P(x_{\min} > x)=\left(1-\frac{x}{m}\right)^N
	\]
	
	and therefore:
	\[
	F_{x_{\min}}(x)=1-\left(1-\frac{x}{m}\right)^N
	\]
	
	Differentiating:
	\[
	p_{x_{\min}}(x)=\frac{d}{dx}F_{x_{\min}}(x)
	=
	\begin{cases}
		\frac{N}{m}\left(1-\frac{x}{m}\right)^{N-1} & \text{if } 0 \leq x \leq m\\
		0 & \text{otherwise}
	\end{cases}
	\]
	
	\item Using the multinomial argument:
	
	We proceed as before by fixing a value $x$ and considering the $k$-th order statistic $x_k$.
	The density of $x_k$ is:
	\[
	p_{x_k}(x)
	=
	\frac{N!}{(k-1)!(N-k)!}
	[F(x)]^{k-1}
	[1-F(x)]^{N-k}
	p(x)
	\]
	
	For the minimum, we set $k=1$. Then there are no points below $x$ and $N-1$ points above it:
	\begin{align*}
		p_{x_{\min}}(x)
		&= N [1-F(x)]^{N-1} p(x) \\
		&= N \left(1-\frac{x}{m}\right)^{N-1}\frac{1}{m} \\
		&= \frac{N}{m}\left(1-\frac{x}{m}\right)^{N-1}
	\end{align*}
	
	which agrees with the previous result.
	
	\item Variable change from $x_{\max}$:
	
	Let $y_i = m - x_i$. Then each $y_i \sim \mathcal{U}[0,m]$ and:
	\[
	y_{\max} = m - x_{\min}
	\]
	
	Since we already know the distribution of $y_{\max}$, we can obtain that of $x_{\min}$ by a change of variables:
	\[
	p_{x_{\min}}(x)
	=
	p_{y_{\max}}(m-x)
	\left|\frac{d}{dx}(m-x)\right|
	\]
	
	which again yields:
	\[
	p_{x_{\min}}(x)
	=
	\frac{N}{m}\left(1-\frac{x}{m}\right)^{N-1}
	\]
\end{itemize}
While we are at it, we can also find expected value and variance for $x_{\max}$ and $x_{\min}$. Starting from $x_{\max}$:
\begin{align*}
	\E_{x_{\max}}[x]&=\int_{0}^{m}x\frac{N}{m}\left(\frac{x}{m}\right)^{N-1}\;dx=\frac{N}{m^N}\int_{0}^{m}x^N\;dx\\
	&=\frac{N}{m^N}\left[\frac{1}{N+1}x^{N+1}\right]^m_0=m \frac{N}{N+1}
\end{align*}
Things are slightly more involved for $x_{\min}$:
\begin{align*}
	\E_{x_{\min}}[x]&=\int_{0}^{m}x\frac{N}{m}\left(1-\frac{x}{m}\right)^{N-1}\;dx\\
	&=\frac{N}{m}m^2\int_0^1y\left(1-y\right)^{N-1}\;dy \quad \left(y=\frac{x}{m}\right)
\end{align*}
where we performed the variable change $y=\frac{x}{m}$. Now we recognize the integral to be a Beta function $\mathrm{B}(2, N)$ (see \ref{sec:beta}). Using the identity that relates Beta to Gamma functions (see \ref{eq:beta_gamma}):
\[
	\mathrm{B}(2,N)=\frac{\Gamma(2)\Gamma(N)}{\Gamma(N+2)}=\frac{1}{(N+1)N}
\]
we obtain\sidenote{The same result can also be obtained via integration by parts. Here, using the Beta function simplifies the computation and illustrates a technique that is often useful in similar integrals.}:
\begin{align*}
	\E_{x_{\min}}[x]&=m\frac{1}{N+1}
\end{align*}

We now compute the second moments, which are useful to determine the variances:
\begin{align*}
	E_{x_{\max}}[x^2]&=\int_0^m x^2 \frac{N}{m}\left(\frac{x}{m}\right)^{N-1}\; dx\\
	&=\frac{N}{m^N}\int_0^mx^{N+1}\;dx\\
	&=\frac{N}{N+2}m^2 \\ \\
	E_{x_{\min}}[x^2]&=\int_0^m x^2 \frac{N}{m}\left(1-\frac{x}{m}\right)^{N-1}\; dx\\
	&=N m^2\int_0^1y^2(1-y)^{N-1}\;dy\\
	&=N m^2 \,\mathrm{B}(3,N) = N m^2 \frac{\Gamma(3)\Gamma(N)}{\Gamma(N+3)} \\
	&=N m^2 \frac{2}{N(N+1)(N+2)} \\
	&=\frac{2}{(N+1)(N+2)} m^2
\end{align*}
Finally for the variances:
\begin{align*}
	\operatorname{Var}_{x_{\max}}[x]&=E_{x_{\max}}[x^2]-E_{x_{\max}}[x]^2\\
	&=\frac{N}{N+2}m^2-\frac{N^2}{(N+1)^2}m^2\\
	&=\frac{N}{(N+1)^2(N+2)}m^2 \\
	\\
	\operatorname{Var}_{x_{\min}}[x]&=E_{x_{\min}}[x^2]-E_{x_{\min}}[x]^2\\
	&=\frac{2}{(N+1)(N+2)} m^2-\frac{1}{(N+1)^2}m^2\\
	&=\frac{N}{(N+1)^2(N+2)}m^2
\end{align*}

Would you look at that, the minimum and the maximum have the same variance! This should be of no surprise, really, as the Uniform distribution, by definition, assigns an equal probability to each value in the interval. Therefore, the probability of extracting a value greater than the current max, or less than the current min should be the same, and so they naturally have the same variance.

\todo{Should we add the Fisher information for $x_{\max}$ and $x_{\min}$?}

\subsection{Normal (Gaussian)}

\subsection{Exponential}

\subsubsection{Truncated exponential}

\subsubsection{Binned exponential}

%Gamma: The generalized version of Exponential.  Add if we ever use

%Beta: For probabilities and proportions.  Add if we ever use

%Cauchy: The "pathological" example (useful to show when moments like mean/variance don't exist!).  Add if we ever use

\section{Sampling distributions}
%Student’s t: For small sample sizes.  Add if we ever use

\subsection{Chi-squared}

%F-distribution: For comparing variances (ANOVA).  Add if we ever use